
import CartItems from '../Components/CartItems/CartItems'; 
// Import the CartItems component from the specified path

const Cart = () => {
  return (
    <div>
      <CartItems /> 
      {/* Render the CartItems component inside a div */}
    </div>
  );
};

export default Cart; 
// Export the Cart component as the default export


